# === Core Server ===
fastapi
gunicorn
uvicorn

# === File Handling & PDFs ===
pdf2image
pillow
python-multipart

# === OCR (Vision Language Model) ===
unsloth
transformers>=4.40.0  # for unsloth compatibility
torch>=2.1.0
accelerate
unsloth>=0.0.25
bitsandbytes>=0.42.0
safetensors

# === Optional for PDF2Image (Poppler dependency) ===
# Note: poppler-utils must be installed at OS level (not in pip)

# === Utility ===
typing-extensions
pydantic>=2.0
uuid
